{
  "name": "gatec-harness-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Gate C Research Harness — 로컬 웹 UI (SPEC §5). runs/ 파일을 읽어 렌더, 자체 상태 없음.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
