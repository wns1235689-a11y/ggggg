import React from 'react'

// 실행 콘솔 — SPEC §5. 이후 청크에서 구현.
export default function Console() {
  return (
    <section className="screen">
      <h2>실행 콘솔</h2>
      <p className="muted">이 화면은 다음 청크에서 구현됩니다 (P2-6 하위: console).</p>
    </section>
  )
}
