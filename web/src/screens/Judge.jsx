import React from 'react'

// 판정 대시보드 — SPEC §5. 이후 청크에서 구현.
export default function Judge() {
  return (
    <section className="screen">
      <h2>판정 대시보드</h2>
      <p className="muted">이 화면은 다음 청크에서 구현됩니다 (P2-6 하위: judge).</p>
    </section>
  )
}
