# SPEC_V3.md — 보정 시뮬 v3-sim (B+ 구성) 사양서

> **지위**: 이 문서는 B+ 작업(보정→적합 검증→카피 변형 1런)의 기준 문서다. INVENTORY/SPEC/INSPECTION/RUNNER와 함께 읽되, v3-sim 범위에서는 본 문서가 우선한다.
> **기준 브랜치**: `claude/runner-md-guide-89tnrn`
> **명명**: "v3-sim"은 **엔진 버전**이다. 설문 버전과 무관하며, 설문은 v2.5가 최종이고 본실행(설문 v3)은 미집행 확정이다.

## §0 목적·산출물 지위

- 목적 2개뿐: ① 실측(파일럿 N=88)으로 보정한 2요인 엔진이 실측의 조건부 패턴을 **한 몸으로 재현하는지** 적합 검증(= 실측→보정→검증 루프의 완결) ② 보정 엔진으로 통제 실험 시연 1건(타마린드 카피 변형).
- 전 산출물 라벨(하드코딩, 비활성화 불가): **"보정 시뮬 · 가설 생성 전용 · 시장 증거 아님 · 인용 불가"**.
- 이 작업은 예측력 검증이 아니라 **구조 충분성 검증**이다. 결합 구조를 주입하므로, 판정 대상은 주입하지 않은 창발 신호로 한정한다(§3·§4 서로소 원칙).

## §1 계보 경계

- **신규 파일만 생성**: build_pool_v3.py · wf_vs_v3.js · v3_analyze.py · v3_variant 관련 · SPEC_V3 부록 A · 최종 보고서. 필요시 harness_paths 재사용.
- **동결(무수정)**: v2.3 엔진 전체(wf_vs_v23.js, wf_v23_multi.js, v23_*·vs_* 분석기), 하니스 UI(백엔드·프론트 — v3 지원은 스코프 밖), 시뮬 문서, 파일럿 보고서, 대조보고서, 설계서 v2.5, data/actual 원본.
- v2.3 코드의 프롬프트 골격·VS 분포화·방향 미주입 원칙·스키마 강제 패턴은 **복사·계승**하여 v3 파일에서 수정한다(원본 무변경).

## §2 2요인 페르소나 설계

- 기존 `향기피` latent에 더해 신규 latent 2종:
  - **경험평가**(과거 카테고리 경험의 부정성): 실측 근거 = 협의 세그가 A1 '별로였다'의 80%·'무경험'의 71% 점유.
  - **해결지향**(장벽 제거 시 전환 동기): 실측 근거 = 세그의 C2 +11%p·D1 +14%p 우위.
- **결합 강도는 임의 설정 금지.** data/actual/게이트C_파일럿_응답.xlsx에서 도출식과 함께 산출하여 **부록 A(도출식·수치·산출 코드 경로)**에 기록한다. 부록 A 커밋은 모든 LLM 런에 선행한다(§6 사전등록 경계).
- **방향 미주입 원칙 유지**: latent와 결합은 주되, 어느 문항에서 어느 보기를 고르는지의 매핑은 프롬프트에 지정하지 않는다(v2.3 원칙 계승). 판정 신호는 LLM 해석의 창발이어야 한다.

## §3 주입 목록 (공개 — 순환 명시)

주변분포 앵커: 협의 유병 33%[24,43] · D1 곡선 수준(5.9k 93%/6.9k 65%/7.5k 28%/8.5k 16%) · C2 주변분포 · B1 전체 평균대. + §2의 2요인 구조·결합 강도(부록 A).
이 항목들은 판정에 사용 금지(순환). 보고서에 "주입 목록"으로 전량 공개.

## §4 판정 목록 (판정 항목은 창발 등급이 상이하다)

> **정정(부록 A 도출 결과 반영 · 소유자 승인 2026-07-25)**: 당초 문구 "주입과 서로소 — 창발만"은 **부정확**했다. 부록 A에서 해결지향 결합의 보정 원천이 §4-2·§4-3의 판정 대상(C2·D1)임이 확정됐고(깨끗한 비판정 대안 프록시는 데이터가 지지하지 않음 — 부록 A 관측 3), ΔB1도 경험평가(A1 유래) 결합으로 완전 매개된다(부록 A A8-2). 따라서 판정 항목은 **서로소가 아니라 창발 등급이 상이하다**. 아래 3계층으로 정의한다.

**창발 3계층**

| 계층 | 항목 | 정의 |
|---|---|---|
| **순수 창발** | #4 B3 비대칭 | 주입에 B3 보기 구성·정통기대 결합이 **없음**(부록 A A8 비주입 선언). 단 경험평가와 B3 정통 우려는 실측 r=−0.30으로 연결 — "직접 주입 없음"이지 "관련 정보 0"은 아님 |
| **좁은 창발** | #1 ΔB1 | 향기피↔경험평가 결합은 주입(A1 유래, 비판정 열). LLM이 "경험평가↑ → 신제품 첫인상↓"로 매핑하는 **1단계 의미 도약**만 검정 |
| **전파 확인(부분 순환)** | #2 ΔC2 · #3 ΔD1 | 결합의 보정 원천이 판정 대상 자신(C2·D1). 검정 내용은 "주입한 결합이 LLM 매핑을 통해 전파되는지"이며 독립 방향 발견이 아님. 구성(construct) 수준의 방향도 latent 이름에 내포 |

**보고 의무**: §8 적합 판정표에 **항목별 창발 등급 컬럼을 필수 병기**한다 — "4/4"라는 헤드라인 등급이 계층 차이를 가리지 않게 한다.

주판정 4개. 각각 **통합 부호 + 3풀 중 2풀 이상 동부호**여야 통과:
1. **ΔB1 음**: 협의 세그(A2a 실현 '매우')−비기피의 B1 가중평균 차 < 0
2. **ΔC2 양**: C1 실현 pick='이런 맛 안 찾음' 페르소나 **제외 조건부**(실측 SKIP 정렬) — **이진 구매율**('꼭 산다'+'가끔 산다' 질량) 차 > 0
   > **정정(소유자 승인 2026-07-25 · 결정 ④=4c)**: 당초 정의 "구매의향(꼭1·가끔0.5·유지0)" **순서점수**는 실측에서 세그 효과가 **+0.038(r=0.12) — 사실상 0**이었다. 세그의 실체는 *"거부가 없음"*(기존유지 0명 · 이진 구매율 100% vs 88.9% = **+11.1%p**)이고 **'꼭 산다'는 오히려 낮아**(15.0% vs 18.5%) 순서점수에서 두 효과가 상쇄된다(부록 A A9-④). 따라서 **판정 지표를 이진 구매율로 사전 지정**하고, 순서점수는 **병기 기록(보조 관찰 · 등급 무관)**으로 남긴다. 조건화(C1 '안 찾음' 실현 제외)는 유지. 대조보고서 §2②의 실측 측도 이진(+11%p)이었으므로 이 정정은 기존 문서와 정렬되며, **어긋나 있던 것은 SPEC_V3 §4-2뿐**이었다.
3. **ΔD1 양**: 총수용개수(buy 4점 합) 차 > 0
4. **B3 비대칭**: '진짜 팟타이 맛 아닐 것' 질량이 세그 < 비기피 (실측 7% vs 31%의 방향 재현 — 순수 창발 테스트)

보조 관찰(등급 무관, 기록만): dose 형태(B1에서 '매우' 최저 재현 여부) · E1 세그 양 보존.

## §5 적합 등급 (사전등록 — 멀티풀 런 전 커밋으로 고정)

- **완전 적합** = 4/4 통과 (B3 포함)
- **부분 적합(구조 지지)** = 3/4 통과이며 B3 포함
- **부분 적합(주변부만)** = 3/4인데 B3 탈락 — "2요인의 핵심 창발 미달" 명시
- **부적합** = ≤2/4
- **카피 변형 런 진행 조건 = 부분 적합(구조 지지) 이상.** 미달 시 변형 런 미집행, 그 자체를 결과로 보고.
- **게이밍 차단**: 적합 판정은 사전 지정 멀티풀 1런으로 확정, 재실행 금지. 기술적 결함(스키마 오류 등)으로 재실행이 불가피하면 사유·횟수를 보고서에 공개. 판정 기준의 사후 조정 불가.

## §6 런 계획·사전등록 경계

**사전등록 경계: SPEC_V3 + 부록 A 커밋 → 그 이후에만 LLM 런.** 순서:
1. dry(N=2) — 파이프라인 확인
2. 단일풀(n≈50, R3급) — **비용 게이트**: 주판정 4개 중 2개 이상 방향 관측 시 진행. 미달 시 조기중단, "부적합(단일풀 조기중단)" 보고로 종료. 이 게이트는 등급 판정이 아니다.
3. 멀티풀(3×100, R4급, 사전 지정 1런) — **등급은 이 런만으로 확정**
4. 구조 지지 이상일 때만: 카피 변형 런(§7)

**예산**: 총 15M 토큰 트립와이어. 도달 시 즉시 중단하고 소요 내역·초과 필요 사유를 보고, 소유자 명시 승인 시에만 증액. 각 런 전에 예상 토큰을 로그에 기록.

## §7 카피 변형 런 (통제 실험 시연 — 탐색 전용)

- **단일 변수 조작, 컨셉 카드만**:
  - 대조군: 원문 그대로 — "…새콤한 맛을 내는 타마린드 대신 매실청을 써서 향 부담을 덜었습니다…"
  - 처치군: "…매실청으로 새콤한 맛을 내 향 부담을 덜었습니다…" (변경점 = "타마린드 대신" 존재 여부. 새콤함·매실청·향 부담 완화 클레임은 양 조건 보존. 그 외 전 문구·전 문항·E1 문안 동일)
- 동일 풀·동일 시드, 단일풀 n≈100 × 2조건. **페르소나 단위 쌍 비교**로 분석 — 단, 쌍 비교가 제거하는 것은 풀 구성 노이즈이며 LLM 샘플링 노이즈는 남는다는 한계를 보고서에 명시.
- 측정: E1 분포 이동(주) · B1(부) · **B3 '맛 상상 안 됨' 질량 이동(탐색)** — "이해 장벽↓ vs 정보량↓" 양방향 트레이드오프 가설, 사전 기대 방향 선언하지 않음.
- **성공/실패 기준 없음.** 어느 방향이든 그대로 보고. 한계 명시: 시뮬 페르소나는 실측의 어휘 이해 실패를 겪지 않을 수 있으므로, 측정 대상은 "단어 존재의 분포 효과"이지 실측 메커니즘의 재현이 아니다.

## §8 최종 보고서 (게이트C_보정시뮬_보고서.md)

구조: ⓪ 지위 라벨(§0) → ① 주입 목록 전량(부록 A 인용) → ② 적합 판정표(주판정 4개 × 통합/풀별 부호, 등급, **항목별 창발 등급 컬럼 필수** — §4) → ③ 보조 관찰(**§4-2 순서점수 병기 포함**) → ④ 변형 런 결과(집행 시) → ⑤ 한계(구조 충분성 검증일 뿐 예측력 아님 · 재실행 이력 · 쌍 비교 한계 · **감쇠 해석 규칙**) → ⑥ 토큰 소요 장부.

**감쇠 해석 규칙 (결과 보기 전 사전등록 · 소유자 승인 2026-07-25 · 결정 ③=3a)**

- 결합은 **비보정**으로 주입한다(부록 A의 실측 β를 latent 공간에 그대로). 실측 β는 *실현↔실현* 결합인데 주입은 *latent* 공간이므로, 시뮬이 다시 실현으로 내려올 때 감쇠가 한 번 더 걸린다 — 부록 A A5에서 **시뮬 실현 세그 간 latent 격차 = 실측 실현 격차의 0.31배**로 측정됨.
- **따라서 사전에 박아 둔다**: **부적합 판정이 나올 경우 "2요인 구조 자체의 실패"와 "감쇠로 인한 검정력 부족"은 이 설계로 구분 불가능하다.** 감쇠는 **후보 설명으로 병기**하며, **재실행 사유가 되지 않는다**(§5 게이밍 차단 유지).
- **3c(단일풀 관측 후 보정 재결정)는 명시 기각** — 결과 의존적 튜닝이라 사전등록 원칙과 양립 불가. **단일풀의 역할은 §6의 비용 게이트뿐**이며 보정 재검토 지점이 아니다.

## §9 세션 운영

- 커밋 프리픽스 `v3:`. 세션 시작 시 본 문서 + 대조보고서 §5(메커니즘)·§3(재질의) + 부록 A(존재 시) 읽기.
- 막히면 — 특히 결합 도출·스키마 설계에서 — 추측하지 말고 [불명확] 질문으로 중단.
- 세션 경계 권장: ① 부록 A 도출 + 엔진 구현 + dry / ② 단일풀→멀티풀 + 등급 판정 / ③ 변형 런 + 최종 보고서. 각 세션 종료 시 토큰 소요 보고.
