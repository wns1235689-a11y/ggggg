# 게이트 C — 시뮬·실측 사후 대조 보고서

## §0 문서 지위 (필수 · 먼저 읽을 것)

- **방법 평가용 사후 대조 · 시장 증거 아님 · 인용 불가.** 이 문서는 시뮬레이션이라는 *도구*가 실측을 얼마나 방향으로 짚었는지를 사후에 점검하는 자료이며, 매실청 팟타이의 시장성에 대한 증거가 아니다.
- **양측 데이터 공개 후의 사후 분석**이므로 자의성(사후 정당화)에 취약하다. 이를 통제하려 3종 장치를 적용했다: ① **전수 대조**(대조쌍을 사전 지정, 유리한 것만 고르지 않음) ② **기계적 부호 판정**(효과크기·근접도 비교 금지, 부호·존재만) ③ **매핑 규칙 선고정**(세그 정의=A2(a) 단독 협의="매우"[설계서 v2.5 §4-3], 지표 정의=엔진 코드값, 재산출 필터=시간컷+B4 정통과).
- 판정 어휘는 6종만 사용: **방향 일치(미확증) / 관측 부호 상반(미확증) / 존재 일치·형태 상이 / 판정 유보 정직 / 대조 불가(순환·조건 불일치) / 맹점.** "적중·검증됨·입증·유의미" 및 효과크기 비교는 쓰지 않는다.
- 시뮬 문서(`게이트C_시뮬결과_정리.md`)·엔진·하니스·파일럿 보고서·설계서 v2.5는 **동결**이며 이 대조로 수정하지 않는다.

**재산출 원칙**: 파일럿 보고서(권위)의 수치를 옮겨 적지 않고 `data/actual/게이트C_파일럿_응답.xlsx`에서 대조 통계를 독립 재산출(검산)했다. 재산출 필터(정본) = **광고 시간컷(direct는 timestamp ≥ 2026-07-17 13:00만 — 태깅 사고 복구) + B4="그렇다"(주의문항 정통과)** → **완주 91**(comm 49 / verify 37 / direct 5) → **유효 N=88**(comm 48 / verify 35 / direct 5). 이 필터로 **협의 세그 n=29·협의 유병률 33.0%**가 보고서와 값까지 일치(§7). 앞선 [불명확#1·#2]는 소유자 정보(시간컷 13:00·설계서 v2.5)로 해소.

---

## §1 순환 제외 목록 (판정 전 선고정)

시뮬의 **앵커 파생량**은 "예측"이 아니라 입력 사전분포의 반영이므로 대조 판정에서 **제외**한다.

| 제외 항목 | 사유 | 앵커 정합(참고 1줄) |
|---|---|---|
| 유병률 **수준** | 시뮬 협의 21~27%·광의 42~47%는 `ANCHOR_PRIORS`(A2 밴드 20~48%) 반영(시뮬 문서 §⑥·§⑪-2). 설계서 v2.5 §4-3도 컷-앵커 매핑을 "시뮬 prior 협의 21%/광의 42%"로 사전 고정 | 실측 협의 33.0%·광의 71.6% — 협의는 밴드 내, 광의는 상한 초과. 시뮬은 "수준 판정 불가" 자인 → 순환, 대조 아님 |
| D1 **절대 수용률** | 시뮬 buy_* 임계표집은 D1 앵커 반영 | 절대값 비교 안 함(가격축 방향만 §2③) |
| B1 **절대 평균** | 시뮬 B1 평균은 B1 앵커(mean/sd) 반영 | 절대값 비교 안 함(세그 차 부호만 §2①) |

---

## §2 대조쌍 판정표 (전수 · 기계적 부호)

세그 = 협의(A2(a) "매우", 설계서 v2.5 §4-3). 시뮬 지표 정의는 엔진 코드값(`v23_multi_analyze.py`: `b1m`=B1 가중평균 / `c2c`=꼭1·가끔.5·유지0 / `d1a`=buy 4점 평균/10 / `e1ng`=(나−가)/10). 실측은 §7에서 독립 재산출(정본 N=88).

| # | 지표 | 시뮬 (문서값) | 실측 (재산출) | **판정** | 감사 메모 |
|---|---|---|---|---|---|
| ① | **B1 첫인상** | r=−0.31(통합·3풀 −0.25/−0.33/−0.32·시드앙상블 세그ΔB1 −0.18±0.09) | ΔB1(세그−비)=**−0.30**(음) | **방향 일치(미확증)** | 시뮬 r(상관) vs 실측 Δ평균(차) = **이종 지표**, 크기 비교 안 함. 부호 양측 음 |
| ② | **C2 전환** | r=−0.34(통합·3풀 일관) | SKIP제외(n=74) 구매% **+11%p**(양) | **관측 부호 상반(미확증)** | 조건 감사: 파일럿 C2는 **C1='안찾음'(SKIP) 제외 조건부**(SKIP⟺안찾음 15/15). 시뮬 C2_dist는 **SKIP 기제 없음**(전원 3지 응답) → 조건 완전 일치 불가. 부호는 조건 무관하게 상반 |
| ③ | **D1 수용** | r=−0.21(통합·풀0 −0.08ns) | D1_7500 **+14%p**·**총수용개수 Δ+0.27**(양) | **관측 부호 상반(미확증)** | 집계 감사: 시뮬 `d1a`=buy 4점 평균/10(총수용). 실측도 **동일 총수용개수** 집계로 재산출 → 그래도 부호 상반(시뮬 음·실측 양) |
| ④ | **E1 (나−가/매실청 메시지)** | r=+0.40(통합·최강·3풀 +0.33/+0.33/+0.53) | 세그 T2 **52%** vs 비 **39%**(+13%p, 양) | **방향 일치(미확증)** | 분모 감사: 시뮬 `e1ng`=(나 질량−가 질량). 실측=세그 T2(나) 선택률. 양측 "세그가 매실청 문구를 더 택함" 부호 양 |
| ⑤ | **'조금' 비단조** | 존재O·형태: 아니다 3.45 / 조금 3.38↓ / **매우 3.53 최고** (R3) | 존재O·형태: 아니다 3.56 / 조금 3.59 / **매우 3.28 최저** | **존재 일치·형태 상이** | 양측 비단조(조금≠매우) 존재. 시뮬은 매우가 **최고**, 실측은 매우가 **최저** — 형태 반대 |
| ⑥ | **E1 전체 분포** | **판정 유보**("뭉갬·표본민감" — R3 나15=가15+뭉갬8, T2 압도 못함) | T2 **43%** > T1 **27%** | **판정 유보 정직** | 시뮬은 n=49 tie를 "압도 못함"으로 유보(확언 안 함). 실측은 T2 우세로 해소 — 유보가 과확언을 피한 정직 사례 |
| ⑦ | **R3-R4 세그교차 화해** | 내부 추론: R3 세그분할 +0.10/+7%p는 "8명 소표본 아티팩트", 연속상관은 이미 음 → 참값 **음** 주장 | 협의 세그 ΔB1 **−0.30**(음) | **방향 일치(미확증)** | 시뮬의 "참 B1 신호는 음" 화해 방향을 실측 음 ΔB1이 지지(역방향이었다면 화해 논리가 반증됐을 자리) |

**판정 분포**: 방향 일치(미확증) ×3(①④⑦) · 관측 부호 상반(미확증) ×2(②③) · 존재 일치·형태 상이 ×1(⑤) · 판정 유보 정직 ×1(⑥).

---

## §3 재질의 (retro-query) — 시뮬 저널 사후 재분석

시뮬 문서는 동결. 아래는 R4 저널(타깃 n=212)을 시뮬 자체 방식(A2a 실현 pick, salt=41 협의컷)으로 재분석한 결과이며 이 보고서에만 존재. 실측 세그 정의(A2(a) 협의)와 동일 매핑.

| 재질의 | 실측 발견 | 시뮬 재산출(R4) | **판정** |
|---|---|---|---|
| **(a) 세그×B3 정통맛의심** | 세그 **7%** vs 비기피 **31%**(강한 비대칭) | '진짜 팟타이 맛 아닐 것'(B3 idx1) 질량 **세그 18% ≈ 비기피 18%**(비대칭 부재) | **시뮬에 부재(표현 불능)** |
| **(b) 세그×A1 저경험 집중** | 협의 세그가 별로였다의 **80%**(8/10)·무경험의 **71%**(5/7) 차지 | 세그 별로+무경험 질량 **32% vs 비기피 24%**(동방향·약함) | **시뮬이 (약하게) 품고 있었으나 미질의** |

- **(a)**: 시뮬은 세그·비기피 B3 분포가 거의 동일 — "세그의 장벽은 맛상상·관심없음, 비기피의 장벽은 정통성"이라는 실측의 **장벽 비대칭(2요인 구조)**을 표현 못 함. B3_dist 필드는 존재하나 단일 향기피 latent가 세그별 장벽 종류 차이를 구동 못 함(§5).
- **(b)**: 시뮬도 세그가 저경험에 **약하게** 더 몰림(32% vs 24%, 동방향)이나 미질의였고, 실측의 강한 집중(80%/71%)엔 못 미침. 방향은 품고 있었음.

---

## §4 맹점 분류 (구조적 불능 vs 미질의)

| 맹점 | 분류 | 근거 |
|---|---|---|
| **타마린드 어휘 장벽**(실측 §6-3) | **구조적 불능** | 시뮬은 **E2 자유서술 채널이 없다**(`wf_vs_v23.js` 스키마=폐쇄형 dist·buy만, E2 필드 부재 — 코드 확인). 자유응답에서만 드러나는 어휘 이해 실패는 표현 공간에 없음. 설계서 v2.5 §2 E2는 실측에만 존재 |
| **채널 이질성**(실측 §6-4) | **구조적 불능** | 시뮬에 **모집·완료율·채널 반응 모형이 없다**(페르소나=인구·latent만, 모집 경로 없음 — 코드 확인). 채널을 층화 변수로 승격할 신호 산출 불가 |
| **거주형태 확장**(실측 §6-6 "6,900이면 가족 세그 동반") | **구조적 불능(근거 확정)** | S3 거주는 `wf_vs_v23.js:35`에 노출되나 **거주×가격 차등이 시뮬에 부재**: R4 재질의 결과 시뮬 D1_7500 **2인가구 44% ≈ 1인가구 44%**(거주-flat, 가격민감 latent가 D1 구동·거주 경로 없음). 실측은 가족 22% vs 1인 35%(차등 존재). 게다가 실측의 "가족 세그" 대상인 **가족거주는 시뮬 1차대상서 제외**(is_target=S3①② 자취/2인, 설계서 v2.5 §4-1) → 표현·판정 양쪽에서 밖 |
| **세그×A1 저경험 집중**(§3b) | **미질의** | §3(b)에서 시뮬이 약하게 품고 있었음 확정(32% vs 24%). 질의만 안 함, 표현 공간 안 |
| **세그×B3 장벽 비대칭**(§3a) | (경계) **구조적 불능** | B3 필드는 있으나 단일 latent가 세그별 장벽 종류 차이 못 냄(§3a 18%≈18%) — 필드 존재·구동 불능 |

---

## §5 메커니즘 분석 — 왜 B1·E1은 방향 일치, C2·D1은 부호 상반인가

검증 가설: **단일 향기피 latent가 B/C/D를 동방향으로 끌게 설계**돼, 실측의 2요인 구조(과거 경험 평가↓ · 해결책 지향↑ 디커플링)를 원리적으로 표현 불능. 문장 단위로 근거 종류를 표기.

- **[코드에서 확인]** 페르소나는 **단일 향기피 스칼라** 하나를 갖는다(`wf_vs_v23.js:35` — `향신료부담 ${lvl(p.향기피)}`, 노출 latent 9종에 "과거 경험 상처"·"해결책 지향" 별도 축 없음).
- **[코드에서 확인]** 방향 매핑은 주입하지 않는다(`wf_vs_v23.js:33` — "어느 특성→어느 보기 매핑은 지정하지 않는다"). 상관은 LLM이 그 단일 latent를 해석해 emergent하게 만든다.
- **[코드에서 확인]** 컨셉 카드가 "타마린드 대신 매실청을 써서 **향 부담을 덜었습니다**"를 명시(`wf_vs_v23.js:47`; 설계서 v2.5 §2 섹션3·§6-8도 이 자극 경로를 한계로 명시). 향기피 페르소나 + 향 부담을 직접 해소하는 카드 → E1 나 선호로 이어지는 **자극 경로**가 프롬프트에 내장.
- **[추론]** 결과적으로 시뮬의 향기피는 "제품 전반 냉담"으로 해석돼 B1·C2·D1을 **함께** 낮추고, 카드가 향 부담을 명시하므로 E1(메시지)만 분리돼 오른다. 즉 시뮬이 만든 디커플링은 **"메시지+ / 행동−"** 한 종류(시뮬 문서 §⑧).
- **[추론]** 실측의 디커플링은 **다른 축**이다 — 세그는 과거 경험 평가는 낮으나(B1↓: §3b 저경험 집중·정통맛의심 아님) 해결책 지향은 높다(C2↑·D1↑: 장벽이 치워지면 전환). 시뮬엔 "과거에 데었으나 동기부여된 해결책 탐색자" latent가 없어(§3a B3 비대칭 부재로 방증), B1↓와 C2/D1↑를 **동시에** 낼 수 없다. 단일 향기피가 B1과 C2/D1을 같은 부호로 묶는다.
- **[추론]** 그래서 부호가 갈린 지점이 정확히 예측된다: **B1은 저경험 채널로 양측 음** → 방향 일치. **E1은 카드 자극 경로로 양** → 방향 일치. **C2·D1은 시뮬이 향기피 냉담으로 음을 내나 실측은 해결책 지향으로 양** → 부호 상반. 이 절이 향후 시뮬 개선 사양(향기피를 "경험 평가"와 "해결책 지향" 2요인으로 분리)의 근거.

---

## §6 설계 기여 이행표 (1차 목표 = 설계 개선의 실현 기록)

시뮬의 1차 목적은 시장 판정이 아니라 설계 결정을 돕는 것. 설계서 v2.5(및 그 안의 v2.4 갱신 로그)가 **시뮬의 역할을 명시적으로 기록**하고 있어, 그대로 인용해 파일럿 회수와 짝짓는다. — **주의**: 설계서는 결정마다 시뮬이 **트리거인지 부기(부수 기록)인지**를 구분하므로 그 구분을 그대로 옮긴다(시뮬 기여 과대평가 차단).

| 설계 결정 | 시뮬의 역할 (설계서 명시) | 파일럿 회수 |
|---|---|---|
| **'조금' 관찰 포인트 등록·3점 확정** | **트리거** — v2.4 갱신 ③ "'조금' 비단조 거동을 파일럿 관찰 포인트로 등록(본실행 2점/3점 결정 기준)", §5 실측표 "시뮬서 비단조 관측(조금 3.38 < 아니다 3.45)" | 파일럿 실측⑥ **비단조 확정**('조금'≠'매우' 두 독립 지표 일관 → 3점·협의컷 실측 정당화) |
| **협의·광의 병기 + 컷-앵커 매핑** | **대조/부기** — v2.4 갱신 ② "컷-앵커 매핑 명시(협의↔21%, 광의↔48%)", §4-3 "두 값의 갭이 클 것으로 예상(시뮬 prior서 협의 21%/광의 42%)되므로 사전 고정" | 파일럿 §3 A2 밴드: **광의 초과(71.6%)는 두 컷 병기 설계가 예정한 판별**, 협의가 세그 정의로 확정(예정된 흡수) |
| **B-1(메시지)·B-2(구매) 이원 분리** | **부기만(트리거 아님)** — v2.4 갱신 ①·§4-5가 명시: "채택 근거는 **시뮬이 아니라 게이트 B 행동 증거와의 정합**", "시뮬 prior(메시지 +0.40 / 구매 −0.31)는 **부기 — prior 할인은 대칭**" | 파일럿 §3 B-2 "혼재 — '발견'으로 기록"(B1 역방향/C2·D1 순방향), §4-5 사전 문구가 단독 철회 아님 → **혼재 결과를 사전 구조가 수용** |

- 관찰: 시뮬 §⑫-5 "가설 재정의(세그가 더 산다 → 세그에 메시지가 꽂힌다)"와 파일럿 §8 목표B "방향 지지·확증 없음" 정직 종료가 **같은 무게중심(메시지 축)**에 놓였다. 단 B-1/B-2 분리의 **1차 근거는 게이트 B**(설계서 명시)이지 시뮬이 아니다 — 이 정합은 방법 관찰이며 시장 증거 아님.

---

## §7 재산출 검산표 (독립 재산출 vs 보고서 v2)

정본 필터 = 광고 시간컷(direct ≥ 2026-07-17 13:00) + B4="그렇다" → N=88. 세그 = A2(a) "매우". 방법: pandas 직접 집계(부트스트랩·CI 미실시 — 부호·존재 판정에 불요). 불일치는 양측 병기 + 플래그.

| 항목 | 보고서 v2 값 | 재산출 값(정본 N=88) | 일치/불일치 |
|---|---|---|---|
| 유효 N | 88 | 88 | **일치** (시간컷+B4로 재구성) |
| 완주 구성 | comm 49·verify 37·광고 5 | comm 49·verify 37·direct 5 | 일치 |
| 협의 세그 n | 29 | 29 | 일치 |
| 협의 유병률 | 33.0% | 33.0% | **일치** |
| B1 세그 Δ | −0.30 | −0.30 | **일치**(음) |
| dose '조금' | 아니다 3.56 · 조금 3.59 · 매우 3.28 | 아니다 3.56 · 조금 3.59 · 매우 3.28 | **일치**(매우 최저·비단조) |
| E1 T2 세그 vs 비 | 52% vs 39%(+13%p) | 52% vs 39%(+13%p) | **일치**(양) |
| E1 전체 T2 vs T1 | 43% vs 27% | 43% vs 27% | **일치**(T2 우세) |
| C2 SKIP제외(n=74) | +11%p | +11%p | **일치**(양) |
| D1_7500 세그 vs 비 | +14%p | +14%p | **일치**(양) |
| B3 정통맛의심 세그 vs 비 | 7% vs 31% | 7% vs 31% | **일치** |
| 세그×A1 저경험 | 별로 80%·무경험 71% | 별로 80%(8/10)·무경험 71%(5/7) | **일치** |

**검산 결론**: 소유자 정보(광고 시간컷 13:00)로 정본 필터를 확정하자 재산출이 보고서 v2의 **전 대조 항목을 값까지 재현**했다(부호·존재뿐 아니라 수치 일치). 광고 시간컷 이전 direct 4행 제외 + B4 3건 제외로 완주 91→유효 88이 정확히 복원됨. 남은 불일치 0. 보고서를 권위로, 재산출을 검산으로 둔 원칙 유지.

---

## §8 종합

**판정 분포**: 방향 일치(미확증) ×3 · 관측 부호 상반(미확증) ×2 · 존재 일치·형태 상이 ×1 · 판정 유보 정직 ×1 · 시뮬 부재(맹점) — 재질의 (a)·§4 3종.

**도구의 불확실성 라벨링 정확성 (미확증 관찰)**: 시뮬이 3풀 일관·시드앙상블로 **가장 강하게 믿은 신호**(E1 +0.40, B1 −0.31)는 실측과 **방향 일치**했고, 시뮬이 스스로 §⑥·§⑪에서 **노이즈 크다고 표기한 축**(C2·특히 D1 "풀0 r=−0.08ns·노이즈 최대")은 실측과 **부호 상반**으로 갈렸다 — 시뮬의 신뢰도 라벨이 사후 대조에서 갈림 지점과 정렬된 것으로 **관측**된다. 이 문장 역시 **미확증**이며, 표본 1건(게이트 C)의 사후 관찰일 뿐 방법의 일반 성질 주장이 아니다.

**메커니즘 요지**: 부호가 갈린 C2·D1은 시뮬의 단일 향기피 latent가 첫인상과 행동을 같은 부호로 묶은 결과이며(§5, 코드 근거), 실측의 2요인(경험 평가↓·해결책 지향↑) 디커플링은 표현 공간 밖이었다(§3a B3 비대칭 부재로 방증). 향후 개선 사양 = 향기피의 2요인 분해.

**[불명확] 집계 (0건 — 초안 3건 전부 해소)**:
1. ~~광고 시간컷~~ → **해소**: 소유자 확인 "13:00 이후" 적용 → direct 유효 5·유효 N=88 값까지 복원(§7).
2. ~~설계서 v2.4/v2.5 원문 부재~~ → **해소**: 설계서 v2.5 첨부 → §6을 v2.4 갱신 로그·§4-3·§4-5로 직접 인용, 시뮬의 트리거/부기 구분까지 반영.
3. ~~§4 거주형태 분류~~ → **해소**: R4 재질의로 시뮬 거주-flat D1(2인 44%≈1인 44%) 확인 + 가족거주 1차대상 제외(v2.5 §4-1) → "구조적 불능"으로 근거 확정.

---

*본 대조는 LLM 호출 0회 · 기존 저널(R3 `wf_9971e46b-6d1`·R4 `wf_6ff10eda-c1f`) 재분석 + xlsx pandas 재산출로 수행. 시뮬 문서·엔진·하니스·파일럿 보고서·설계서 v2.5는 동결·무수정. 방법 평가용이며 시장 증거로 인용 불가.*
