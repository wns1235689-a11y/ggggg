# SPEC_V3 부록 A — 2요인 결합 강도 도출

> **지위**: SPEC_V3 §2가 요구한 결합 강도의 실측 도출 기록. **보정 시뮬 · 가설 생성 전용 · 시장 증거 아님 · 인용 불가.**
> **사전등록 경계(SPEC_V3 §6)**: 본 부록의 **승인 커밋 이후에만** LLM 런(dry 포함) 허용. 본 문서 작성 시점까지 LLM 호출 **0회**.
> **입력**: `data/actual/게이트C_파일럿_응답.xlsx`(동결 원본) · 기존 v2.3 R4 런 `runs/wf_6ff10eda-c1f`(동결 저널, 재분석만).
> **산출 코드(신규)**: `derive_coupling_v3.py` — 재현: `HARNESS_RUNS=$PWD/runs python3 derive_coupling_v3.py --json /tmp/coupling_v3.json`
> **상태**: §A9 [불명확] 4건 **결정 완료**(①1a ③3a ④4c) → 확정값 §A11. 독립 감사 정정 4건 §A10-2. v2.3 기준선 §A12. **미결 1건 = 요인 간 상관 주입 여부(§A13)**.

---

## A0. 표본 · 척도 (정본)

- 필터 = 대조보고서 §0 정본: 광고 시간컷(`src=='direct'`는 `timestamp ≥ 2026-07-17 13:00`) + `B4=='그렇다'` → **N=88**(comm 48 / verify 35 / direct 5).
- 세그 정의 = 협의 **A2a=='매우'**(설계서 v2.5 §4-3, A2(a) 단독) → **n=29 (33.0%)** / 비기피 n=59.
- 향기피 강도 척도 `a2int` = A2a→{아니다 0, 조금 0.5, 매우 1.0} — **출처 `v23_analyze.py` a2_intensity 가중치 계승**(신규 척도 발명 없음).
  - 전체 μ=0.523 · σ=0.393. 세그 평균 1.000 / 비기피 0.288 → **Δa2int = 0.712** (β_shift의 분모).

## A1. 도출 대상 = 샘플러의 선형 조건부 시프트 계수 β

기존 계보 관용구를 그대로 계승한다(`sim/sampler.py:43` — `lat["sauce_barrier"] += 0.3*(spice_aversion-0.5)`):

```
경험평가 = clip( N(μ_b^exp, σ_b^exp) + β_exp × (향기피 − 0.5) , 0, 1)
해결지향 = clip( N(μ_b^sol, σ_b^sol) + β_sol × (향기피 − 0.5) , 0, 1)
```

즉 **"결합 강도" = (β_exp, β_sol)**. 두 추정식을 병기한다(A3):

| 추정식 | 정의 | 성질 |
|---|---|---|
| **β_shift** | (세그 평균 − 비기피 평균) / Δa2int | 세그 이분 대조를 단위 향기피당 시프트로 환산. §4 판정이 세그 대조라 판정 지표와 정렬 |
| **β_ols** | cov(a2int, 프록시)/var(a2int) | 3수준 전체를 쓰는 회귀 기울기. '조금'을 중간값으로 취급(선형 가정) |

## A2. 요인 프록시 조작화 (실측 관측 열 → 요인)

| 요인 | SPEC §2 근거 | 프록시 열 | 조작화 |
|---|---|---|---|
| **경험평가**(과거 카테고리 경험의 부정성) | 협의 세그가 A1 '별로였다' 80%·'무경험' 71% 점유 | **A1** | 좋아한다 0 / 보통이다 0.5 / 별로였다 1.0 / **안먹어봄 = 코딩 갈림** → 0.50·0.75·1.00·이진 4변형 전수 산출(A3) |
| **해결지향**(장벽 제거 시 전환 동기) | 세그의 C2 +11%p · D1 +14%p | **C2·D1** | C2 순서점수(꼭1·가끔0.5·유지0, SKIP 제외 n=74) / D1 총수용개수÷4 / 두 지표 평균 |
| (해결지향 대안 후보) | — | A2c·A2x·C1·B2_1 | §A9-①의 서로소 보존 옵션 검토용 |

## A3. 결합 강도 추정치 (전수)

지위 열: **비판정** = §4 판정 대상이 아닌 열에서 도출 / **판정대상** = §4가 판정하는 열에서 도출(순환 쟁점 → §A9-①).

| 프록시 | 지위 | n | 세그 | 비기피 | Δ | **β_shift** | **β_ols** | r_p | r_s |
|---|---|---|---|---|---|---|---|---|---|
| 경험평가/A1[무경험0.50] | 비판정 | 88 | 0.500 | 0.136 | +0.364 | **0.512** | 0.426 | 0.48 | 0.48 |
| **경험평가/A1[무경험0.75]** | 비판정 | 88 | 0.543 | 0.144 | +0.399 | **0.561** | 0.461 | 0.49 | 0.49 |
| 경험평가/A1[무경험1.00] | 비판정 | 88 | 0.586 | 0.152 | +0.434 | **0.609** | 0.495 | 0.49 | 0.49 |
| 경험평가/A1[이진(별로∪무경험)] | 비판정 | 88 | 0.448 | 0.068 | +0.381 | **0.534** | 0.417 | 0.41 | 0.42 |
| 해결지향/C2 순서점수(SKIP제외) — *보조 기록* | 판정대상 | 74 | 0.575 | 0.537 | +0.038 | 0.053 | 0.077 | 0.12 | 0.11 |
| **해결지향/C2 이진 구매율(SKIP제외)** ★ | 판정대상 | 74 | 1.000 | 0.889 | **+0.111** | **0.154** | 0.168 | 0.24 | 0.24 |
| 해결지향/D1 총수용÷4 | 판정대상 | 88 | 0.552 | 0.483 | +0.069 | 0.097 | 0.129 | 0.19 | 0.20 |
| 해결지향/D1 총수용÷4 — 공통 n=74 한정 | 판정대상 | 74 | — | — | — | 0.186 | — | 0.283 | — |
| 해결지향/C2순서·D1 복합지표 | 판정대상 | 74 | 0.600 | 0.514 | +0.086 | 0.119 | 0.132 | 0.24 | 0.25 |
| **해결지향/C2이진·D1 복합지표** ★**확정 원천** | 판정대상 | 74 | 0.812 | 0.690 | **+0.123** | **0.170** | 0.178 | 0.32 | 0.31 |
| 해결지향대안/A2c 접근성장벽 | 비판정 | 88 | 0.466 | 0.500 | −0.035 | −0.048 | 0.020 | 0.02 | 0.02 |
| 해결지향대안/A2x(아니오=1) | 비판정 | 88 | 0.759 | 0.186 | +0.572 | 0.804 | 0.688 | 0.56 | 0.56 |
| 해결지향대안/C1 탐색중(안찾음=0) | 비판정 | 88 | 0.690 | 0.915 | −0.226 | −0.317 | −0.274 | −0.29 | −0.29 |
| 해결지향대안/B2 매실청1순위 | 비판정 | 88 | 0.241 | 0.186 | +0.055 | 0.077 | 0.081 | 0.08 | 0.08 |
| 해결지향대안/**A2b 낯선재료 부담**(감사 지적·A10-2 정정2) | 비판정 | 88 | — | — | — | — | — | **0.61** | — |
| 해결지향대안/A2d 가격 부담(동) | 비판정 | 88 | — | — | — | — | — | −0.08 | — |

★ = 소유자 결정 ④(4c)로 판정 지표가 **이진 구매율**로 확정됐으므로 **도출도 동일 지표로 일관 적용**(도출·판정 정합, 소유자 조건 ④-③). 확정 원천 = **C2이진·D1 복합지표**(#2·#3 둘 다 반영).

**β_shift 부트스트랩 95% CI** (B=10,000 · seed=20260725):

| 프록시 | β_shift | 95% CI | 0 포함 |
|---|---|---|---|
| 경험평가/A1[무경험0.75] | 0.561 | **[0.334, 0.778]** | 미포함 |
| 해결지향/C2 이진 구매율 ★ | 0.154 | **[0.050, 0.278]** | 미포함 |
| **해결지향/C2이진·D1 복합 ★** | **0.170** | **[0.059, 0.285]** | 미포함 |
| 해결지향/D1 총수용(n=88) | 0.097 | [−0.080, 0.267] | **포함** |

주: 이진 전환은 β_sol을 0.053(순서점수) → **0.154~0.170**으로 **강화**하며 CI가 0을 배제한다(D1 단독 n=88만 0 포함 — 복합 원천을 확정 원천으로 쓰는 근거).
> **척도 정합 주의(v3_analyze 구현용)**: 실측 D1은 개인별 4가격 예/아니오 → 총수용 0~4(÷4로 [0,1] 정규화). 시뮬 D1은 `buy_<가격>` = 10명 중 수용 인원 0~10 → 기존 `v23_multi_analyze.d1a`가 `mean(buy)/10`으로 [0,1] 정규화. **양측 모두 [0,1]로 정규화하면 Δ 부호·상대 비교가 정합**하며, §4-3의 "총수용개수(buy 4점 합)"는 이 정규화로 구현한다(스케일 4 vs 40 혼용 금지).

**관측 1 — 경험평가 결합은 강하나, 점추정의 정밀도는 낮다.** β_shift 0.512~0.609(SPEC §2가 근거로 든 저경험 해석 범위 = 무경험 0.50~1.00 및 이진), r 0.41~0.49.
- **정밀도 한계(독립 감사 지적, 반영)**: β_shift=0.561의 부트스트랩 95% CI = **[0.337, 0.785]**(N=88). **소수 셋째자리는 이 표본이 지지하지 않는다** → 구현 시 유효자릿수 2자리(0.56)로 쓰고, 민감도는 CI 양단(0.34 / 0.79)으로 별도 기록할 것을 권고.
- **견고성의 범위 한정**: 위 범위는 열거한 4개 코딩에 한한다. 범위 밖에서는 이탈 — 무경험 0.25 → 0.463, 무경험 0.00 → 0.415, 안먹어봄 listwise 제거 → 0.531. 즉 "'안먹어봄'을 저경험으로 본다"는 SPEC §2의 해석 자체가 β의 하한을 정한다.

**관측 2 — 해결지향 결합은 약하고, 표본 정의에 민감하다.** §4가 판정하는 지표들에서 β_shift 0.053~0.186 (r 0.12~0.28) — 경험평가의 1/3~1/10.
- 순서점수 기준 0.053(n=74) · D1 총수용 0.097(n=88)·0.186(n=74) · **이진 기준 0.154~0.170** ← 확정.
- **해석 정밀화(소유자 조건 ④-②)**: 세그의 C2 효과의 실체는 **"거부의 부재"**다 — 기존유지 **0명**, 이진 구매율 100% vs 88.9%(+11.1%p). 그런데 **'꼭 산다'는 오히려 낮다**(15.0% vs 18.5%). 따라서 **해결지향 latent의 의미는 "열광적 전환"이 아니라 "거부 소거(장벽이 치워지면 거절하지 않음)"로 읽어야 한다.** wf_vs_v3.js의 특성 서술과 v3_analyze의 해석 문구는 이 정의를 따른다.
- **표본 민감성(독립 감사 지적, 반영)**: D1 결합은 n=88에서 0.097이나 C2 응답자만의 공통 n=74에서 **0.186(r=0.283, p=0.015)로 약 2배**. SKIP(=C1 '안찾음', 세그에 9/29로 집중)을 제외하면 세그 우위가 강해진다 — "카테고리에서 이탈한 하위집단이 D1 신호를 희석"하는 구조. β_sol 선택 시 **어느 표본 정의를 쓸지 함께 지정해야 한다**(→ §A9-④에 병합).
- 위 "복합지표"는 응답자별 (C2순서점수 + D1총수용)/2 (n=74)이며, C2·D1 각 β의 산술평균이 아니다.

**관측 3 — 해결지향의 '비판정' 대안 프록시는 데이터가 지지하지 않는다.**
- `A2x`: r=0.56로 가장 강하지만 **향기피와 준-동어반복**(설계서 v2.5 §4-4는 A2x를 A2 정합 검증 앵커로 규정 / 시뮬 문서 §⑨도 "향기피↑→A2x"를 준-동어반복 등급). 제2요인이 아니라 향기피의 복제 → **부적격**.
- `C1 탐색중`: **부호 반대**(세그 69.0% vs 비기피 91.5%). 세그는 현재 카테고리에서 오히려 이탈해 있다(안찾음 9/29 vs 5/59). 게다가 C1은 §4-2의 조건화 변수 자신.
- `A2c`(r=0.02)·`B2 매실청1순위`(r=0.08): 무신호.

## A4. 요인 간 구조 — ⚠ **이 절의 결론은 §A10-2 정정 1로 반증됨**(원문 보존)

> **경고**: 아래 0.089는 **순서점수 프록시** 기준이며, 확정 원천(이진)으로는 **+0.161**이다. 표본도 n=74이고 배제된 14명이 경험평가 최상위군이다. 주입 여부는 **미결(§A13)** — 반드시 §A10-2 정정 1과 함께 읽을 것.

| 항목 | 값 |
|---|---|
| corr(경험평가, 해결지향) | 0.185 |
| **partial corr | a2int 통제** | **0.089** |
| corr(a2int, 경험평가) | 0.493 |
| corr(a2int, 해결지향) | 0.243 |

향기피를 통제하면 두 요인의 잔차 상관은 0.089로 무시 가능 → **샘플러는 두 요인을 향기피에 각각 독립적으로 결합시키면 충분**하고, 요인 간 별도 상관항은 주입하지 않는다. (이는 실측이 "경험평가↓와 해결지향↑가 같은 사람 안에서 **디커플링된 채** 공존"한다는 대조보고서 §5 메커니즘과 정합.)

## A5. 감쇠 계수 — latent ↔ 실현 (기존 R4 런 재분석, n=300)

| 항목 | 값 |
|---|---|
| corr(latent 향기피, **믿음질량** a2int) | **0.828** |
| corr(latent 향기피, **실현 pick** 강도) | 0.490 → ⚠ **정정 ≈0.44 ±0.05 [0.37,0.52]**(§A10-2 정정 3 — 0.49는 12 draw 중 상단) |
| sd(latent 향기피) | 0.225 |
| sd(믿음질량 a2int) / sd(실현 pick) | 0.207 / 0.385 |

### A5-2. 감쇠 사슬 명시 문서화 (소유자 조건 ③-①)

**사슬 구조**:
```
[실측] 실현 A2a ──(β 관측: 실현↔실현)── 실현 프록시(A1·C2·D1)
[시뮬] latent 향기피 ──(β 주입: latent↔latent)── latent 요인 ──(LLM 매핑)── 믿음질량 응답
                     ↑ 세그 정의는 '실현 A2a pick'으로 되돌아감(ρ_x=0.49)
```
- **x측(세그 정의) 감쇠**: 세그는 실현 A2a pick으로 가른다 → corr(latent, 실현)=0.49.
- **y측(결과) 감쇠**: 판정 지표(b1m·c2 질량·d1a)는 **믿음질량**이라 의미연결이 강하면 감쇠가 작다. 실측 앵커: corr(latent 가격민감, 믿음질량 d1a) = **−0.777**(강한 매핑 가능) vs corr(latent 향기피, 믿음질량 b1m) = **−0.135**(약한 매핑) — 즉 y측 감쇠는 0.14~0.78 사이로 **의미연결 강도에 좌우**된다.

**예상 감쇠 폭 — 직접 측정(추정 아님)**: R4 런에서 시뮬의 **실현 세그 간 latent 향기피 격차**를 재고, 실측의 실현 격차와 대조했다.

| 항목 | 값 |
|---|---|
| 시뮬 실현 세그(협의, n=34) latent 향기피 평균 | 0.542 |
| 시뮬 비기피(n=178) latent 향기피 평균 | 0.319 |
| **Δlatent(시뮬)** | **0.223** |
| Δa2int(실측 실현 척도) | 0.712 |
| **격차 배율 = 0.223 / 0.712** | **0.313 (≈31%)** — 앙상블 검증 0.32±0.04 [0.26,0.37] (§A10-2 정정 3: 견고) |

⇒ **동일 β를 비보정으로 주입하면 시뮬의 요인 세그 격차는 실측 프록시 격차의 약 31% 수준**이 된다:

| 요인 | 주입 β | 시뮬 요인 세그 격차 | 실측 프록시 격차 |
|---|---|---|---|
| 경험평가 | 0.56 | **+0.125** | +0.399 |
| 해결지향 | 0.17 | **+0.038** | +0.123 |

**함의(사전등록)**: 이 감쇠는 §4의 세그 정의(실현 pick)에서 구조적으로 발생하며 보정 없이는 제거 불가. 특히 **해결지향의 시뮬 격차 +0.038은 프롬프트 노출 단계의 `lvl()` 5단계 버킷 폭(≈0.16)보다 작아**, 다수 페르소나에서 버킷이 바뀌지 않는다 → **#2·#3은 검정력이 희박할 수 있다.** 이 예상은 결과 이전에 기록되며, SPEC_V3 §8의 감쇠 해석 규칙(부적합 시 구조 실패와 검정력 부족은 구분 불가·감쇠는 후보 설명·재실행 사유 아님)이 이에 대응한다. 보정 선택지는 §A9-③에서 **3a(비보정) 확정 · 3b·3c 기각**.

## A6. 근거 행렬 (교차표 · N=88)

**A2a × A1** (경험평가 결합의 원천)
| A2a | 좋아한다 | 보통이다 | 별로였다 | 안먹어봄 | 계 |
|---|---|---|---|---|---|
| 아니다 | 21 | 3 | 0 | 1 | 25 |
| 조금 | 24 | 7 | 2 | 1 | 34 |
| **매우** | 8 | 8 | **8** | **5** | 29 |

**A2a × C2**
| A2a | 꼭산다 | 가끔산다 | 기존유지 | SKIP |
|---|---|---|---|---|
| 아니다 | 4 | 16 | 4 | 1 |
| 조금 | 6 | 22 | 2 | 4 |
| **매우** | 3 | 17 | **0** | 9 |

**A2a × D1 수용률(%)**
| A2a | 5,900 | 6,900 | 7,500 | 8,500 |
|---|---|---|---|---|
| 아니다 | 88.0 | 52.0 | 16.0 | 12.0 |
| 조금 | 97.1 | 70.6 | 29.4 | 14.7 |
| 매우 | 93.1 | 69.0 | **37.9** | 20.7 |

세그 vs 비기피 가격별 Δ: 5,900 **−0.1%p** / 6,900 +6.3 / 7,500 **+14.2** / 8,500 +7.1 — 세그 우위는 **고가 구간에 국지적**이고 저가에서는 없다.

**A2a × B3** (판정 #4의 대상 — 주입 아님)
| A2a | 맛상상안됨 | 정통맛의심 | 냉동불신 | 가격 | 양부족 | 관심없음 | 없음 |
|---|---|---|---|---|---|---|---|
| 아니다 | 4 | **10** | 3 | 3 | 3 | 1 | 1 |
| 조금 | 8 | 8 | 5 | 4 | 4 | 3 | 2 |
| **매우** | **11** | **2** | 3 | 3 | 3 | 7 | 0 |

**A2a × C1** / **A2a × A2x**
| A2a | 배달외식 | 냉동밀키트 | 직접 | 안먹음 | **안찾음** | | A2x 예 | A2x 아니오 |
|---|---|---|---|---|---|---|---|---|
| 아니다 | 14 | 5 | 3 | 2 | 1 | | 23 | 2 |
| 조금 | 18 | 8 | 2 | 2 | 4 | | 25 | 9 |
| 매우 | 14 | 1 | 0 | 5 | **9** | | 7 | **22** |

## A7. 신규 latent 주변분포 (μ, σ) 후보 + 분산 분해

프록시 주변분포(실측):

| 프록시 | μ_total | σ_total |
|---|---|---|
| 경험평가/A1[무경험0.75] | 0.276 | 0.368 |
| 경험평가/A1[이진] | 0.193 | 0.397 |
| 해결지향/C2·D1 평균 | 0.537 | 0.211 |
| 해결지향/C2 순서점수 | 0.547 | 0.251 |
| 해결지향/D1 총수용÷4 | 0.506 | 0.263 |

샘플러의 base 분포는 결합항의 분산을 뺀 값이어야 총 주변분포가 실측과 맞는다:

```
μ_b = μ_total − β × (μ_향기피 − 0.5)
σ_b = sqrt( σ_total² − β² × σ_향기피² )
# μ_향기피 = 0.346 · σ_향기피 = 0.2249 — R4 풀(n=300) latent 실현값 직접 측정
#   (LATENT_SPECS 명세 μ=0.34·σ=0.25에 파생 시프트 −0.2×(매실청−0.5)와 [0,1] 클리핑이 적용된 뒤의 값)
```

| 요인 | β | μ_b | σ_b |
|---|---|---|---|
| 경험평가(무경험0.75) | β_shift 0.561 | 0.362 | 0.346 |
| 경험평가(무경험0.75) | β_ols 0.461 | 0.347 | 0.353 |
| 해결지향(C2·D1 복합) | β_shift 0.119 | 0.555 | 0.209 |
| 해결지향(C2·D1 복합) | β_ols 0.132 | 0.557 | 0.209 |

주: 해결지향은 β가 작아 결합항의 분산 기여가 거의 없다(σ_b ≈ σ_total). 이 분해는 "실측 프록시 주변분포를 latent 주변분포로 그대로 옮긴다"는 가정 위에 있다(→ §A9-③).

## A8. 비주입 선언 (판정 #4의 순수성 보존 장치)

다음은 **의도적으로 주입하지 않는다.** 주입하면 §4 판정이 순환이 된다.

1. **정통기대(authenticity_goal) ↔ 향기피 결합 없음** — 실측의 B3 비대칭(비기피가 정통 우려)을 latent로 심으면 #4가 순환이 되므로, 이 두 latent는 무상관으로 유지한다.
2. **어느 문항의 어느 보기를 고르는지의 매핑 미지정** — v2.3 원칙(`wf_vs_v23.js:33` "어느 특성→어느 보기 매핑은 지정하지 않는다") 계승.
3. **B3 보기 구성·E1 선호 방향·dose 형태에 대한 지시 없음.**

**참고(주입 아님) — LLM이 창발로 재현해야 하는 실측 매핑 강도**:

| 실측 상관 | 값 | 의미 |
|---|---|---|
| corr(경험평가, B1) | **−0.304** | 판정 #1이 통과하려면 LLM이 재현해야 하는 경로 |
| corr(a2int, B1) | −0.124 | 향기피 **자체**의 B1 상관은 약함 → ΔB1(−0.30)은 주로 **경험평가 경로**의 산물 (2요인 분해의 실측 정당화) |
| corr(해결지향, B1) | +0.169 | 반대 부호 — 두 요인이 B1에서 상쇄 |
| corr(경험평가, B3 정통맛의심) | **−0.298** | ⚠ #4는 "직접 주입 없음"이지 "관련 정보 0"이 아니다(§A9-② 기록) |
| corr(경험평가, B3 맛상상안됨) | 0.029 | 세그의 **주** 장벽(맛상상)은 경험평가로 설명되지 않음 → 이 부분은 순수 창발 |
| corr(a2int, B3 정통맛의심) | −0.309 | — |

### A8-2. ΔB1의 매개 구조 — 2요인 분해의 실측 정당화 (그리고 #1이 실제로 검정하는 것)

B1 회귀(정본 N=88):

| 모형 | 계수 | R² |
|---|---|---|
| B1 ~ a2int | a2int **−0.294** | 0.015 |
| B1 ~ exp | exp **−0.772** | 0.092 |
| **B1 ~ a2int + exp** | a2int **+0.082** · exp **−0.815** | 0.093 |
| B1 ~ a2int + exp + sol (n=74) | a2int −0.016 · exp −0.159 · sol +0.704 | 0.032 |

- 편상관: **corr(a2int, B1 | exp) = +0.031** (단순 −0.124) / corr(exp, B1 | a2int) = −0.281 (단순 −0.304).
- A1 층별 ΔB1(세그−비기피): 좋아한다 −0.04 · 보통이다 +0.35 · 별로였다 −0.75(비기피 셀 n=2) · 안먹어봄 +0.30 — 층 내부에 일관된 향기피 효과 없음(셀 과소, 참고).

**해석 1(설계 정당화)**: 실측 ΔB1 −0.30은 **경험평가 구성으로 완전 매개**된다. 향기피를 통제하면 자체 효과는 0(계수 부호 반전). 이는 대조보고서 §5가 추론했던 "단일 향기피로는 B1↓와 C2/D1↑를 동시에 못 낸다"를 실측으로 확인하며, **2요인 분해의 실측 근거**가 된다.

**해석 2(판정 #1의 정직한 범위)**: 그 결과 #1(ΔB1 음)이 검정하는 것은 **"LLM이 경험평가↑ → 신제품 첫인상↓로 매핑하는가"**의 1단계 의미 도약뿐이다(향기피→경험평가 결합은 주입). 창발이지만 **좁은 창발**이며, "시뮬이 ΔB1을 스스로 알아냈다"가 아니다 — 보고서 §②에 이 문장을 라벨로 넣는다.

### A8-3. 판정·보조 관찰의 실측 기준선 (v3_analyze 대조용 · 주입 아님)

| SPEC_V3 항목 | 실측(정본 N=88) 값 | 시뮬이 재현해야 하는 것 |
|---|---|---|
| #1 ΔB1 | **−0.30** (세그 3.28 / 비기피 3.58) | 부호 음 |
| #2 ΔC2 | 순서점수 **+0.038** / 이진 구매율 **+11.1%p** | 부호 양 (지표 선택 → §A9-④) |
| #3 ΔD1 | 총수용÷4 **+0.069**(n=88) / **+0.19**(n=74) · 7,500 **+14.2%p** | 부호 양 |
| #4 B3 비대칭 | 정통맛의심 세그 **6.9%**(2/29) < 비기피 **30.5%**(18/59) | 세그 < 비기피 |
| 보조: dose 형태 | B1 아니다 3.56 · 조금 3.59 · **매우 3.28(최저)** | '매우' 최저의 비단조 |
| 보조: E1 세그 양 | 세그 T2 **52%** vs 비기피 **39%** (+13%p) | 세그 T2 우위 보존 |

## A9. [불명확] 4건 — **전부 결정 완료** (소유자 승인 2026-07-25)

> **결정 요약**: ① **1a**(SPEC대로 보정 + 창발 3계층 라벨, §5 무변경) · ② 기록으로 충분 · ③ **3a**(비보정, 3b·3c 기각) · ④ **4c**(판정=이진 구매율, 순서점수 병기, 도출도 이진 일관).
> 반영 커밋: `v3: §4 창발 계층 정정`(3df8d88) · `v3: §4-2 지표 정정`(83bfed3) · `v3: §8 감쇠 해석 규칙 사전등록`(dfe5336). 아래는 결정 전 검토 기록(원문 보존).

### ① [핵심·결정 필요] 해결지향 결합의 보정 원천 — 순환 vs 서로소

**충돌**: SPEC_V3 §2는 해결지향의 근거로 **"세그의 C2 +11%p·D1 +14%p"**(=§4-2·§4-3의 **판정 대상**)를 지목한다. 그 원천으로 β_sol을 보정하면, #2·#3은 "주입한 결합이 LLM 매핑을 통해 전파되는지"의 **전파 확인**이 되어 §0·§4의 *주입과 판정은 서로소* 원칙과 충돌한다. 반면 #1(ΔB1)은 원천이 A1(비판정 열)이라 창발성이 유지되고, #4(B3)는 원천이 없어 가장 순수하다.

**A3 관측 3이 닫은 길**: 서로소를 지키면서 해결지향을 보정할 **깨끗한 비판정 프록시는 데이터에 없다**(A2x 준-동어반복 / C1 부호반대·조건화 변수 / A2c·B2 무신호).

| 선택지 | 내용 | 트레이드오프 |
|---|---|---|
| **1a** SPEC 문자대로 | β_sol = C2·D1 유래(0.053~0.132 중 지정) | §2 충실. 단 #2·#3의 라벨을 "전파 확인(부분 순환)"으로 강등해야 하고 §5 등급(4/4=완전적합)의 의미가 바뀜 → §5 개정 필요 |
| **1b** 비보정 관례값 | β_sol을 데이터 아닌 관례로 고정(예: β_exp와 동일, 또는 0.10) | ΔC2·ΔD1의 **크기**는 미보정 유지. 단 §2의 "결합 강도 임의 설정 금지"와 정면 충돌 |
| **1c** 판정 재구성 | β_sol은 1a대로 보정하되 **#2·#3을 보조 관찰로 이동**, 주판정은 #1·#4(+dose·E1) | 서로소 원칙 보존. §4·§5 개정 필요. 등급 분모가 4→2(또는 4개 중 창발 2개 가중)로 바뀜 |

> **참고**: 어느 선택이든 "구성(construct) 수준의 방향"은 이미 이름에 내포된다 — 해결지향 = "장벽 제거 시 전환 동기"이고 컨셉 카드가 곧 장벽 제거이므로, #2·#3의 부호는 결합 원천과 무관하게 준-명목적일 수 있다. 이 한계는 어느 선택지에서도 남으며 보고서에 기록해야 한다.

### ② [기록으로 충분] #4 B3 창발성의 정확한 등급
경험평가(주입)와 B3 정통맛의심은 실측에서 r=−0.298로 연결돼 있다. 따라서 #4의 정직한 라벨은 **"B3 보기 구성에 대한 직접 주입 없음 · 단 경험평가 경로와 실측 상관 존재"**이며 "완전 무관"이 아니다. 세그의 주 장벽(맛상상안됨)은 경험평가와 무상관(0.029)이라 그 부분은 순수 창발. → 결정 불요, 보고서 라벨에 반영.

### ③ [결정 필요·경미] 감쇠 보정 여부
| 선택지 | 내용 | 트레이드오프 |
|---|---|---|
| **3a** 비보정(권고) | A3의 β를 latent 공간에 그대로 | 투명·추정 위 추정 없음. 시뮬 실현 결합이 실측보다 **약하게** 나와 보수적(자기 불리) 검정. 단 너무 약하면 부호가 노이즈에 묻혀 위-부적합 위험 |
| **3b** ρ 상향 보정 | β/ρ 류로 확대(ρ_belief 0.828 또는 ρ_realized 0.49) | 시뮬 실현 결합을 실측에 맞춤. 단 보정계수 자체가 추정이고 프록시 측 감쇠는 미지 → 임의성 유입 |

### ④ [결정 필요·경미] §4-2 지표 정의 — 순서점수 vs 이진 구매율
§4-2는 **순서점수**(꼭1·가끔0.5·유지0)로 정의한다. 그런데 실측에서 그 지표의 세그 효과는 **+0.038(r=0.12) — 사실상 0**이다. 이유가 구조적이다:

| C2 지표 | 세그 | 비기피 | Δ |
|---|---|---|---|
| 이진 구매율(꼭+가끔) | **100.0%** (20/20) | 88.9% | **+11.1%p** |
| 꼭산다 비율 | **15.0%** | 18.5% | **−3.5%p** |
| 순서점수(§4-2) | 0.575 | 0.537 | **+0.038** |

세그의 실체는 **"거부가 없음"**(기존유지 0명)이지 **"더 열성적"**이 아니다 — 순서점수는 이 둘을 상쇄시켜 신호를 지운다.

| 선택지 | 트레이드오프 |
|---|---|
| **4a** §4-2 유지(순서점수) | 사양 충실. 목표 효과가 ≈0이라 "3풀 중 2풀 동부호" 요구가 노이즈 지배 → 판정이 사실상 동전던지기 |
| **4b** 이진 구매율로 변경 | 실측 헤드라인과 정렬. 단 세그 100%는 **천장**이라 시뮬이 초과 불가·비대칭 검정 |
| **4c** 둘 다 산출·통과 기준은 사전 지정 1개 | 정보 최대·게이밍 차단 유지. 사전 지정이 필수 |

## A10. 재현

```bash
cd /home/user/ggggg
HARNESS_RUNS=$PWD/runs python3 derive_coupling_v3.py --json /tmp/coupling_v3.json
```
- 입력 동결 파일만 읽음(쓰기 없음), LLM 호출 0회.
- 표·수치는 위 출력과 1:1 대응. 감쇠 계수는 `runs/wf_6ff10eda-c1f` 재분석.

## A10-2. 독립 적대 감사 반영 정정 (읽기전용 서브에이전트 9종 · 원 기술은 삭제하지 않고 정정 명시)

부록 A 초안의 6개 주장을 독립 재도출로 반증 시도한 결과 **5 CONFIRMED · 1 REFUTED**. 정정 4건:

### (정정 1) **A4 결론 반증** — 요인 간 상관은 "무시 가능"이 아니다 ⚠

| 해결지향 프록시 | corr(경험평가) | **a2int 통제 편상관** | n | 지위 |
|---|---|---|---|---|
| **C2이진·D1 복합 ★확정 원천** | +0.280 | **+0.161** | 74 | 확정 |
| C2순서·D1 복합(초안이 인용) | +0.185 | +0.089 | 74 | 보조 |
| C1 탐색중 | −0.671 | **−0.633** | 88 | 기각(§A3 관측 3) |
| A2x(아니오=1) | +0.716 | **+0.612** | 88 | 기각(준-동어반복) |
| D1 단독 | +0.065 | −0.035 | 88 | 부분 |

- **초안 오류**: A4가 인용한 0.089는 **순서점수 기준**이었다. 확정 원천(이진)으로는 **+0.161**이다.
- **추가 취약성(감사 지적)**: 복합지표는 C2 응답자에서만 정의돼 **n=74**이고, 배제된 14명은 **무작위 결측이 아니다** — 경험평가 평균 **0.839 vs 비배제 0.169**(A1 구성: 별로였다 7 · 안먹어봄 5 · 보통 2). 즉 "경험평가 최상위군의 해결지향은 관측되지 않았다". 편상관 +0.161은 선택된 부표본의 값이며 편향 방향은 미확정.
- **프록시 의존성**: 기각된 프록시로는 |편상관| 0.61~0.63까지 간다(단 C1은 §4-2의 조건화 변수 자신, A2x는 향기피와 준-동어반복이라 제2요인 자격 없음 — 기각 논거는 §A3 관측 3과 동일).
- ⚠ **엔진 영향 → 소유자 결정 필요**: 현행 `build_pool_v3.py`는 두 요인을 (향기피 조건부) **독립**으로 뽑는다. +0.161을 주입하려면 `해결지향 += γ×(경험평가−0.5)`, γ ≈ 0.161×(0.22/0.35) ≈ **0.10**. **임의 변경 금지 원칙(§2)에 따라 자동 반영하지 않았다.** 판단 재료는 §A13.

### (정정 2) A3 관측 3의 후보 열거 누락 — A2b·A2d 추가

| 열 | corr(향기피) | corr(해결지향★) | 판정 |
|---|---|---|---|
| **A2b**(낯선 소스·재료 부담) | **+0.614** | +0.335 | **부적격** — 데이터셋 최강 비판정 상관이지만 **제2의 장벽 차원**(기피 계열)이다. 향기피와 준-동어반복(파일럿 §5: a-협의 29명 중 15명이 b-협의 겸함)이며 "장벽 해소 시 거부 소거"라는 해결지향 구성과 무관 |
| A2c(접근성) | +0.024 | +0.172 | 무신호(초안 기재 유지) |
| A2d(가격) | −0.082 | −0.026 | 무신호 |

초안은 A2 배터리 형제항목(A2b·A2d)을 열거하지 않았다 — **결론(깨끗한 비판정 프록시 부재)은 유지되나 그 논거가 문서화되지 않았던 결함**을 정정한다.

### (정정 3) A5 감쇠 계수 — 고정시드 단일 draw → 앙상블 밴드

| 항목 | 초안(고정시드) | **정정(앙상블 e=0..11)** |
|---|---|---|
| ρ_realized = corr(latent 향기피, 실현 A2a) | 0.49 | **≈0.44 ± 0.05 [0.37, 0.52]** — 0.49는 12 draw 중 최댓값 근처(유리한 draw) |
| Δlatent(시뮬 실현 세그 격차) | 0.223 | 0.227 ± 0.027 [0.188, 0.264] |
| **감쇠 배율(Δlatent/0.712)** | 0.31 | **≈0.32 ± 0.04 [0.26, 0.37]** — 초안 값은 밴드 내·평균 근접(**견고**) |

⇒ A5-2의 결론(시뮬 요인 격차는 실측의 약 1/3)은 **유지**된다. ρ_realized 표기만 0.44로 정정.

### (정정 4) A8 B3 누출량 — 단순상관이 아니라 **증분** 기준

| 항목 | 값 |
|---|---|
| corr(경험평가, B3 정통) 단순 | −0.298 |
| **corr(경험평가, B3 정통 \| a2int 통제)** | **−0.176** |
| corr(a2int, B3 정통 \| 경험평가 통제) | −0.195 |

경험평가와 향기피가 r=0.493으로 중복되므로, **경험평가 주입이 새로 추가하는 누출은 −0.176**이며 나머지는 v2.3이 이미 갖고 있던 향기피 경로다. v2.3 기준선(§A12)이 B3에서 −0.000(비대칭 부재)을 낸 사실이 이를 뒷받침 — **#4의 순수 창발 지위는 정정 후에도 유지**되며, 오히려 초안보다 강해진다.

## A11. 확정값 (소유자 승인 반영 · 이 절의 커밋이 SPEC_V3 §6 사전등록 경계의 기준선)

### A11-1. build_pool_v3.py 주입 파라미터 (확정)

```python
# 결합 강도 — 부록 A A3 β_shift, 유효자릿수 2자리(CI 폭 반영)
BETA_EXP = 0.56   # 향기피 → 경험평가   (원천: A1 무경험0.75 · CI [0.334,0.778])
BETA_SOL = 0.17   # 향기피 → 해결지향   (원천: C2이진·D1 복합 · CI [0.059,0.285])

# 신규 latent 주변분포
LATENT_V3 = {
    "past_experience_negativity": (0.36, 0.35),  # 경험평가 base: 실측 A1 주변분포에서 분산 분해
    "solution_orientation":       (0.50, 0.22),  # 해결지향 base: 중립 특성 prior(아래 근거)
}
```

| 파라미터 | 값 | 근거 |
|---|---|---|
| β_exp | **0.56** | A3 β_shift 0.561(무경험0.75). CI [0.334,0.778] → 3자리 무의미하므로 2자리 |
| β_sol | **0.17** | A3 ★확정 원천(C2이진·D1 복합) β_shift 0.170. CI [0.059,0.285], 0 배제 |
| 경험평가 (μ_b, σ_b) | **(0.36, 0.35)** | 실측 A1 주변분포(μ 0.276·σ 0.368)에 분산 분해 적용(A7) → (0.362, 0.346) |
| 해결지향 (μ_b, σ_b) | **(0.50, 0.22)** | **프록시 주변분포를 쓰지 않는다** — 아래 근거 |

**해결지향 주변분포를 중립 prior로 두는 근거(기록 · 이견 시 정정 가능)**: 경험평가의 프록시(A1)는 **특성 자체**(과거 경험 평가)를 직접 재고, A1 분포는 §3 주입 목록에 없어 보정해도 이중 계산이 아니다. 반면 해결지향의 프록시(C2·D1)는 **특성의 결과(응답)**이고, 그 **수준(주변분포)은 이미 §3에서 별도 주입**된다(C2 주변분포·D1 곡선 수준). 프록시 주변분포(이진 기준 μ=0.72~0.92, 천장 근접)를 latent μ로 옮기면 같은 정보를 두 번 주입하고 latent가 천장에 붙어 변별력을 잃는다. 따라서 **결합 강도(β)만 실측 보정하고, 주변분포는 기존 LATENT_SPECS 관용구의 중립값**(involvement (0.50,0.22)와 동형)을 쓴다. σ_b는 분산 분해 적용 시 0.217 → 0.22로 반영.

### A11-2. 확정 사항 — 판정·해석 규칙

| 항목 | 확정 내용 | 근거 |
|---|---|---|
| 창발 계층 | #4 순수 창발 / #1 좁은 창발 / #2·#3 전파 확인(부분 순환). 보고서에 항목별 등급 컬럼 필수 | 결정 ①=1a · SPEC §4 정정(3df8d88) |
| §5 등급 체계 | **무변경**(4/4 완전적합 · 3/4+B3 구조 지지 · B3 필수 · 변형런 조건) | 결정 ①-③ |
| §4-2 지표 | **이진 구매율**(꼭+가끔) Δ>0로 판정, 조건화 유지. 순서점수는 보조 기록 | 결정 ④=4c · SPEC §4-2 정정(83bfed3) |
| 해결지향 의미 | "열광적 전환"이 아니라 **"거부 소거"** | 결정 ④-② · A3 관측 2 |
| 감쇠 | **비보정**(3a). 예상 격차 배율 0.31 기록. 부적합 시 구조 실패·검정력 부족 구분 불가 → 감쇠는 후보 설명·재실행 사유 아님 | 결정 ③=3a · SPEC §8 사전등록(dfe5336) |
| 3b·3c | **명시 기각** — 3b는 추정 위 추정, 3c는 결과 의존적 튜닝(단일풀은 §6 비용 게이트 전용) | 결정 ③-③ |
| 요인 간 상관 | 주입하지 않음(partial corr 0.089) | A4 |
| 비주입 유지 | 정통기대↔향기피 무결합 · 보기 매핑 미지정 | A8 |

### A11-3. 사전등록 경계 발효

본 절을 포함한 커밋 이후 LLM 런 허용(SPEC_V3 §6): dry(N=2) → 단일풀(비용 게이트) → 멀티풀(등급 확정, 사전 지정 1런) → 조건 충족 시 변형 런.

---

## A12. v2.3 기준선 — 단일요인 엔진을 v3 판정 기준으로 채점 (사전 기록 · LLM 0회)

v3의 순증을 측정할 대조군. **기존 R4 저널(v2.3, 동결)** 에 `v3_analyze.py`를 그대로 적용했다. v3 런 이전에 기록해 사후 체리피킹을 차단한다.

재현: `HARNESS_SP=$PWD/runs/wf_6ff10eda-c1f HARNESS_JOURNAL_BASE=$PWD/runs python3 v3_analyze.py wf_6ff10eda-c1f`

| 판정 | 지표 | 통합 Δ | 기대 | 풀 부호 | 통과 | 창발 등급 |
|---|---|---|---|---|---|---|
| #1 | ΔB1(가중평균) | **−0.180** | 음 | −−+ | **통과** | 좁은 창발 |
| #2 | ΔC2(이진 구매율·조건부) | −0.032 | 양 | −−+ | 미달 | 전파 확인(부분 순환) |
| #3 | ΔD1(총수용 정규화) | −0.051 | 양 | −−+ | 미달 | 전파 확인(부분 순환) |
| #4 | B3 '진짜 팟타이 아닐것' 질량 | **−0.000** | 음(세그<비기피) | +−+ | 미달 | 순수 창발 |

**v2.3 기준선 = 1/4 · 부적합** (타깃 n=212 · 세그 34 / 비기피 178). 보조: dose '매우' 최저 **True**(v2.3도 재현) · C2 순서점수 Δ −0.028 · E1 나−가 Δ +0.081(세그 나질량 35.6% vs 32.2%).

**해석 기준(사전 고정)**: v3가 #4(B3)를 통과하면 그것은 v2.3이 −0.000으로 내지 못한 신호이므로 **2요인 구조에 귀속**된다. #2·#3의 부호 반전도 같은 방식으로 귀속 가능하되, 그 둘은 '전파 확인(부분 순환)' 계층임을 병기한다(§4).

## A13. 미결 1건 — 요인 간 상관 주입 여부 (소유자 결정 요청)

정정 1(A10-2)에서 드러난 사안. **엔진 파라미터가 바뀌므로 임의 반영하지 않았다**(§2 임의 설정 금지).

| 선택지 | 내용 | 트레이드오프 |
|---|---|---|
| **13a 미주입(현행·권고)** | 두 요인을 향기피 조건부 독립으로 유지 | ① 추정이 취약: n=74이고 배제된 14명이 **경험평가 최상위군**(0.839 vs 0.169) — 선택 부표본의 값 ② 보수적: 추가 결합 없이 패턴이 재현되면 구조 주장이 더 강하고, 실패 시 "미주입 결합"이 감쇠와 함께 후보 설명으로 남음 ③ 초안이 선언한 materiality 임계(0.2) 미달 |
| **13b 주입(γ≈0.10)** | `해결지향 += 0.10×(경험평가−0.5)` 추가 | ① **실측 스토리에 더 충실** — "과거에 데었으나 장벽 치우면 전환"이라는 미충족 수요 구조가 바로 이 양(+) 결합이다 ② 단 #2·#3(이미 전파 확인 계층)로 가는 경로를 한 겹 더 주입 ③ 추정 취약성을 그대로 승계 |

권고: **13a**(현행 유지). 단 어느 쪽이든 A10-2 정정 1의 취약성 서술을 최종 보고서 §⑤ 한계에 인용할 것.
